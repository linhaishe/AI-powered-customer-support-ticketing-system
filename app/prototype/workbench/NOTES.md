# Prototype Notes

Question: What should the first Tianmao Global AI customer service workbench look like?

This is throwaway prototype code. It explores three structurally different UI directions for the first-phase workbench described in `docs/prd.md`.

Run:

```bash
npm run dev
```

Open:

```txt
http://localhost:3000/prototype/workbench?variant=A
http://localhost:3000/prototype/workbench?variant=B
http://localhost:3000/prototype/workbench?variant=C
```

Verdict placeholder:

- Winning variant:
- What to keep:
- What to discard:
- What to fold into production:
